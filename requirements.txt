Pillow==12.3.0
requests==2.34.2
watchdog==6.0.0

# Exact transitive dependencies of requests; keep these aligned with its metadata.
certifi==2026.7.22
charset-normalizer==3.5.1
idna==3.18
urllib3==2.7.0

# RPi.GPIO-compatible gpiochip provider, including Raspberry Pi 5 support.
rpi-lgpio==0.6; platform_machine == "aarch64" or platform_machine == "armv7l" or platform_machine == "armv6l"
